/** @type {import('next').NextConfig} */
const nextConfig = {
    // async rewrites() {
    //     return [
    //       {
    //         source: '/api/:path*',
    //         destination: 'https://lcpotdbackend.onrender.com/api/:path*' // Change this to your server's URL
    //       },
    //     ]
    //   }
};

export default nextConfig;
